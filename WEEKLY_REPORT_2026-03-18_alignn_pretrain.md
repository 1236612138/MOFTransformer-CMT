# 本周周报：基于大规模 CIF 数据的 ALIGNN-MOFTransformer 预训练方案推进

## 1. 本周工作概述

本周围绕 `ALIGNN` 图编码版 `MOFTransformer` 预训练方案，完成了从原始 `CIF` 结构数据到自监督预训练数据集的整体链路梳理，并落地了纯 `CIF` 场景下的数据预处理脚本与后台运行方案。当前工作的核心目标，是在缺少大规模公开实验吸附标签的前提下，充分利用海量结构数据，先完成结构表征层面的自监督预训练，为后续 `N2/O2` 吸附预测任务提供更强的初始化模型。

本周已经明确：现阶段最有价值的数据资源不是少量带标签的吸附数据，而是规模达到 `1,118,074` 个的 `CIF` 结构库。基于这一判断，当前路线调整为优先完成大规模结构预处理与自监督预训练，而不是直接依赖有限的下游监督标签做从头训练。

## 2. 数据现状与利用策略

### 2.1 当前可用数据

当前已确认目录 `/home/yihaoyu/dataset/primitive_cif` 中共有 `1,118,074` 个 `.cif` 文件。  
这些数据的主要特点如下：

- 数据规模足够大，适合作为自监督预训练语料。
- 数据仅包含结构信息，不包含与 `N2/O2` 吸附量直接对应的标签。
- 文件已经是 `primitive cif` 形式，适合当前 `ALIGNN` 图侧编码逻辑继续使用。

### 2.2 为什么当前不直接以实验标签为主

经过调研，公开可获得的“`CIF` 结构 + 实验吸附标签”数据规模远小于结构库本身：

- `MOF` 公开实验结构库可达上万规模，但真正能可靠配对实验吸附标签的数据通常只有几百到低几千。
- `COF` 公开实验结构和实验吸附标签的规模更小，通常为几百级。
- 公开实验 isotherm 数据虽不少，但存在结构配对困难、测试条件不统一、重复样本多、数据清洗成本高等问题。

因此，如果当前直接依赖实验标签开展下游监督训练，会面临以下问题：

- 可用样本量小，难以支撑从头训练大型模型。
- 数据分布窄，泛化能力容易受限。
- 结构库的规模优势无法被充分利用。

基于以上原因，当前更合理的数据利用策略是：

1. 用海量 `CIF` 做自监督预训练。
2. 用 `RASPA` 或其他计算方法构造中等规模伪标签数据。
3. 再用少量实验数据做最终微调和校正。

## 3. 当前技术路线

### 3.1 总体路线

当前项目建议采用三阶段训练路线：

1. `CIF` 自监督预训练  
2. `RASPA` / 文献标签监督微调  
3. 少量实验标签校准微调

这一路线的核心思想是：先让模型学会“结构表示”，再学习“结构到吸附性能”的映射，最后再向真实实验分布靠拢。

### 3.2 当前正在执行的阶段

目前正在推进的是第一阶段：  
基于 `1,118,074` 个 `CIF` 文件，构建 `ALIGNN-MOFTransformer` 的纯结构自监督预训练数据集。

在该阶段中，数据被处理为：

- 图结构文件：`.graphdata`
- 能量网格文件：`.grid`
- 网格数值文件：`.griddata16`
- 数据切分索引：`train.json`、`val.json`、`test.json`

这里的 `train/val/test.json` 仅承担索引作用，其中目标值统一使用占位值，不作为真正监督标签使用。

### 3.3 当前预训练任务选择

当前选择只开启以下两类自监督任务：

- `ggm`：Graph Grid Matching
- `mpp`：Masked Patch Prediction

这两个任务的优点是：

- 不依赖任何下游标签。
- 直接利用结构图与能量网格之间的多模态对应关系。
- 能够同时强化图编码器和网格编码器的表征能力。

当前不启用以下任务：

- `mtp`
- `vfp`
- `moc`
- `bbc`

原因是这些任务均需要额外标签文件，而现阶段尚未构建对应的高质量标签集。

## 4. 本周已完成工作

### 4.1 数据预处理链路重构

为适配当前纯 `CIF` 自监督预训练目标，本周新增并调试了纯结构数据预处理脚本：

- [prepare_alignn_pretrain_data.py](/home/yihaoyu/docker/bag/MOFTransformer_upgrade/prepare_alignn_pretrain_data.py)

该脚本与原有下游任务数据准备脚本不同，不再依赖 `N2/O2` 等下游标签，而是直接面向纯 `CIF` 数据完成：

- 图结构生成
- 能量网格生成
- 训练/验证/测试集划分
- 纯预训练所需的占位 JSON 文件生成

### 4.2 后台运行与日志系统

为适配大规模数据预处理场景，本周同时完成了后台运行脚本：

- [run_prepare_alignn_pretrain_data.sh](/home/yihaoyu/docker/bag/MOFTransformer_upgrade/run_prepare_alignn_pretrain_data.sh)

该脚本支持：

- 后台持续运行
- 断开 VS Code / SSH 后任务继续执行
- 日志持续落盘
- 进度信息定期输出

对应日志目录为：

- [logs](/home/yihaoyu/docker/bag/MOFTransformer_upgrade/logs)

### 4.3 预处理速度优化

针对最初串行预处理速度过慢的问题，本周已完成以下优化：

- 将预处理逻辑改为多进程并行
- 支持 `num_workers` 与 `chunksize` 参数配置
- 增加流式进度输出
- 日志中定期输出完成量、速率和 ETA
- 兼容后台运行场景
- 屏蔽部分高频低价值 warning，降低日志噪声

当前运行日志显示，预处理速率约为 `3.2 - 4.8 cif/s`，估算完整处理时间约为 `3 - 4` 天量级。虽然耗时仍较长，但已从最初串行估算的数十天显著降低到可接受范围。

## 5. 对数据利用的理解与判断

### 5.1 当前这批 CIF 数据的价值

这批 `1,118,074` 个 `CIF` 的最大价值，不在于它们能直接作为吸附预测训练标签，而在于它们足以支撑一个大规模结构自监督预训练过程。

具体来说，这批数据可以帮助模型学习：

- 不同 MOF/COF 框架的拓扑与局部配位模式
- 图结构与能量网格之间的对应关系
- 多尺度结构特征的共享表征
- 后续下游气体吸附任务所需的结构先验

### 5.2 为什么先做自监督预训练是合理的

在当前缺少大规模实验标签的情况下，先做自监督预训练具有明显优势：

- 能充分利用现有海量结构数据。
- 可以显著降低后续下游任务对标签规模的依赖。
- 有利于提高小样本监督微调的稳定性和泛化能力。
- 能为 `N2/O2` 吸附任务提供更强的参数初始化。

因此，当前“先把全部 `CIF` 预处理好，再进行自监督预训练”的方向是合理且必要的。

## 6. 后续模型训练方向

### 6.1 第一阶段：完成自监督预训练

短期内应继续完成以下工作：

- 持续完成全部 `CIF` 的预处理
- 基于 `ggm + mpp` 完成 `ALIGNN-MOFTransformer` 自监督预训练
- 输出可复用的预训练 checkpoint

这一阶段的目标不是直接预测吸附值，而是得到一个强结构表征模型。

### 6.2 第二阶段：构造下游吸附监督数据

在完成预训练后，下一阶段建议优先构建 `N2/O2` 吸附任务的监督数据集。当前更现实的数据来源包括：

- `RASPA` 计算得到的大规模模拟标签
- 已有项目中的 `N2/O2` 历史数据
- 少量可清洗的文献实验标签

其中，`RASPA` 标签的价值主要在于：

- 数据量更容易扩展
- 与结构数据可直接配对
- 适合作为中间监督信号

### 6.3 第三阶段：实验标签微调与校准

若后续能够逐步整理到一定规模的实验 `N2/O2` 吸附数据，建议在已完成的预训练模型和 `RASPA` 微调模型基础上，进一步做实验校准微调。该阶段的作用是：

- 修正模拟标签与实验标签之间的系统偏差
- 提升模型对真实实验分布的适配能力
- 提高最终模型在真实任务中的可信度

## 7. 当前存在的问题

当前仍存在以下问题与风险：

- 全量 `CIF` 预处理耗时较长，资源占用较大。
- 公开实验标签规模有限，短期内难以直接支撑大规模实验监督训练。
- `RASPA` 标签虽然可扩展，但本质上仍是模拟结果，不能完全替代实验真值。
- 后续如果要面向真实应用，仍需要实验数据进行校准。

## 8. 下周计划

下周计划如下：

1. 持续完成大规模 `CIF` 数据预处理，并检查失败样本比例。  
2. 启动 `ALIGNN-MOFTransformer` 的 `ggm + mpp` 自监督预训练。  
3. 整理现有 `N2/O2` 下游数据集，明确可用于监督微调的标签来源。  
4. 评估后续采用 `RASPA` 标签扩充中间训练集的可行性。  
5. 为后续实验标签微调阶段预留数据接口与训练流程。  

## 9. 本周结论

本周工作的核心结论如下：

- 当前最值得优先利用的数据资源是大规模 `CIF` 结构库，而不是有限的实验标签。
- 将全部 `CIF` 预处理后用于自监督预训练，是当前最合理的技术路线。
- 对于 `N2/O2` 吸附预测任务，建议采用“自监督预训练 + 模拟标签监督微调 + 少量实验校准”的三阶段方案。
- 当前数据预处理与后台运行链路已经基本打通，为下一步正式预训练创造了条件。

